services:
  db:
    image: postgres:16
    container_name: carsharing-db
    environment:
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: carsharing
    ports:
      - "5432:5432"
    volumes:
      - carsharing_data:/var/lib/postgresql/data

volumes:
  carsharing_data: